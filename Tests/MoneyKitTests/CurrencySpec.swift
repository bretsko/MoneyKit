
import Quick
import Nimble
@testable import MoneyKit

class CurrencySpec: QuickSpec {
  override func spec() {
    describe("Currency") {
      it("can init ") {
        
        //expect(sections).to(contain("aa"))
      }
    }
  }
}
