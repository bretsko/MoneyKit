
import Quick
import Nimble
@testable import MoneyKit

class MoneySpec: QuickSpec {
  override func spec() {
    describe("Money") {
      it("can init ") {
        
        //expect(sections).to(contain("aa"))
      }
    }
  }
}
